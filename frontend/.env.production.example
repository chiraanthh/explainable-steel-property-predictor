# Copy this file to `.env.production` and set your deployed backend URL before
# running `npm run build`. The value is baked into the build at compile time.
#
#   cp .env.production.example .env.production
#
VITE_API_BASE_URL=https://YOUR-RENDER-SERVICE.onrender.com
